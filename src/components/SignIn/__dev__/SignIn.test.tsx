/* eslint-env jest */
import { render } from '@testing-library/react';
import SignIn from '../SignIn';

describe('SignIn', () => {
  test('it renders as expected', () => {
    render(<SignIn />);
  });
});
