import styled from 'styled-components/macro';

export const TrumpCard = styled.div`
  position: absolute;
  top: 120px;
  right: 40px;

  img {
    border-radius: 16px;
    box-shadow: rgba(0, 0, 0, 0.5) 3px 3px 3px;
  }
`;
