import * as S from './Players.styles';

export type PlayersProps = {}

/* Players */
export default function Players(props: PlayersProps) {
  const {
    /* destructured props */
  } = props;

  return (
    <S.Players>
      This is the styled & stateless Players component.
    </S.Players>
  );
};
/* */

