import * as S from './Hand.styles';

export type HandProps = {}

/* Hand */
export default function Hand(props: HandProps) {
  const {
    /* destructured props */
  } = props;

  return (
    <S.Hand>
      This is the styled & stateless Hand component.
    </S.Hand>
  );
};
/* */

