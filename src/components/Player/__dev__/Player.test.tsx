/* eslint-env jest */
import { render } from '@testing-library/react';
import Player from '../Player';

describe('Player', () => {
  it('renders as expected', () => {
    render(<Player />);
  });
});
